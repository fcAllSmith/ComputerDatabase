package com.excilys.computerdb.fconsigny.presentation.validation;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.time.format.DateTimeParseException;

public class ValidateEntries {
  
  public static boolean isCorrectEntries(ComputerDto computerDto) {
    
    return true;
  }

  public static LocalDateTime stringToLocalDate(String inputDate) {
    if (inputDate == null) {
      return null;
    }

    try {
      DateTimeFormatter formatter;
      LocalDate date;
      formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd");
      date = LocalDate.parse(inputDate, formatter);
      return date.atStartOfDay();
    } catch (DateTimeParseException e) {
      return null;
    }
  }

  public static boolean isValideId(long inputId) {
    if (inputId > -1) {
      return true;
    }

    return false;
  }

  public static boolean isDate(String inputDate) {
    if (inputDate != null) {
      if (!inputDate.matches("-")) {
        return true;
      }
    }

    return false;
  }

}
